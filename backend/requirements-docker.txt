# knowledge-system 后端 Docker 依赖
# 注意：这是手写的「干净的」依赖清单，勿用 `pip freeze` 覆盖，以免带入本地路径和版本冲突。
# 关键版本锁定以与代码/迁移兼容为准，次要传递依赖交给 pip 自动解析。

# ---- Web 框架 ----
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
email-validator
python-dotenv

# ---- ORM / 数据库（MySQL + SQLite）----
tortoise-orm==0.25.4
aerich==0.9.2
aiomysql
aiosqlite
pydantic>=2.6.0
pydantic-settings
# pydantic-core 不锁定，交由 pip 选择与 pydantic==2.5.0 兼容的版本

# ---- 安全 / 认证 ----
bcrypt==3.2.2
passlib==1.7.4
python-jose[cryptography]
rsa

# ---- 爬虫 / HTML 解析 ----
httpx
requests
beautifulsoup4
lxml
readability-lxml
html2text
cssselect

# ---- 文档 / 文件解析与导出 ----
aiofiles
python-docx
python-pptx
pdfplumber
PyPDF2
Pillow
XlsxWriter
python-dateutil
pytz

# ---- AI 供应商 ----
zai-sdk==0.2.2
openai>=1.0.0